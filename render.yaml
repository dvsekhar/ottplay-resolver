services:
  - type: web
    name: ottplay-resolver
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: python server.py
